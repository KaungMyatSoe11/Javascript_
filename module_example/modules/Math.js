// export const Mul = (x, y) => x * y;

// export const Divided = (x, y) => x / y;

// export const Modulus = (x, y) => x % y;

 const Mul = (x, y) => x * y;

 const Divided = (x, y) => x / y;

 const Modulus = (x, y) => x % y;

 module.exports={Mul,Divided,Modulus}


